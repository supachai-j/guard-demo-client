# Pinned to 0.136.1 (not the latest 0.136.3) to avoid MAL-2026-4750:
# the 0.136.3 release tampered with [project.optional-dependencies].standard
# to inject an undocumented `fastar>=0.9.0` typosquat. We don't install
# fastapi[standard] so the typosquat doesn't actually reach our wheel set
# — but a clean release tree avoids the audit failure AND avoids future
# `pip install fastapi[standard]` accidents on dev machines.
fastapi==0.136.1
uvicorn[standard]==0.24.0
# Pin starlette explicitly so fastapi's loose dep gets a CVE-clean version.
# CVE-2024-47874, CVE-2025-54121 fixed in 0.47.2; CVE-2025-62727 fixed in 0.49.1.
# PYSEC-2026-161 (Host header injection -> path confusion) fixed in 1.0.1;
# requires fastapi>=0.126 to allow starlette 1.x.
starlette==1.0.1
python-multipart==0.0.28
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
sqlalchemy==2.0.23
chromadb==0.4.18
openai>=1.40.0
# litellm 1.84.0+ relaxes its strict python-dotenv / pydantic pins,
# letting us pull in the CVE-clean versions of both.
litellm>=1.84.0
# pydantic 2.10+ required by recent litellm; pydantic-settings bumped to match.
pydantic>=2.10.0,<3.0.0
pydantic-settings>=2.5.0,<3.0.0
python-dotenv==1.2.2
aiofiles==23.2.1
pypdf2==3.0.1
markdown==3.8.1
pandas==2.1.4
requests==2.34.2
httpx==0.28.1
boto3
reportlab>=4.0
